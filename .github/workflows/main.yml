
name: CI
on:
  push:
    branches: [ main ]

  workflow_dispatch:


jobs:
  website_update:
    runs-on: ubuntu-latest
    steps:

      - name: Git clone my repo
        uses: actions/checkout@v2

      - name: Current folder
        run: pwd
        
      - name: List folder
        run: ls -la
